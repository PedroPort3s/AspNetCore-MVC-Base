@{ 
    ViewData["Title"] = "Bem-Vindo";
}

<h2>Bem-Vindo</h2>

<ul>
    @for (int i = 0; i < (int)ViewData["NumVezes"]; i++)
    {
        <li>
            @ViewData["Mensagem"]
        </li>
    }
</ul>

